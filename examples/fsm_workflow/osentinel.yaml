# FSM engine config — deterministic workflow enforcement.
#
# The model field is for the semantic classifier fallback (embedding similarity).
# If omitted, auto-detected from API keys like every other engine.

engine: fsm
policy: ./customer_support.yaml
port: 4000
